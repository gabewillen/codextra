---
phase: 01-background-trigger-and-continued-turns
plan: 04
type: execute
wave: 3
depends_on:
  - 01-03
files_modified:
  - codex-rs/core/tests/suite/compact.rs
  - codex-rs/core/tests/suite/compact_remote.rs
  - codex-rs/core/tests/suite/user_shell_cmd.rs
  - codex-rs/app-server/tests/suite/v2/compaction.rs
autonomous: true
requirements:
  - RUN-02
user_setup: []
must_haves:
  truths:
    - Tests prove automatic mid-turn compaction starts without extra user action and agent progress continues while compaction is in flight for both local and remote paths.
    - Tests prove background auto-compaction does not emit replacement-task lifecycle and stays single-flight per active turn in Phase 1.
    - App-server compaction coverage, if still applicable, reflects background execution ordering without asserting later-phase persistence or replay semantics.
  artifacts:
    - codex-rs/core/tests/suite/compact.rs
    - codex-rs/core/tests/suite/compact_remote.rs
    - codex-rs/core/tests/suite/user_shell_cmd.rs
    - codex-rs/app-server/tests/suite/v2/compaction.rs
  key_links:
    - Local and remote core suites validate the runtime behavior introduced in `codex-rs/core/src/codex.rs`.
    - App-server compaction tests only cover the Phase 1 event surface and intentionally avoid Phase 2+ transcript correctness assumptions.
---

<objective>
Lock Phase 1 runtime behavior behind targeted concurrency and lifecycle regression tests.

Purpose: Phase 1 changes core turn orchestration, so the final plan in the phase must prove the agent keeps running during background compaction and that no replacement-task regressions are introduced.
Output: Local, remote, active-turn invariant, and app-server regression coverage for the Phase 1 background-trigger behavior.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-background-trigger-and-continued-turns/01-RESEARCH.md
@.planning/phases/01-background-trigger-and-continued-turns/01-VALIDATION.md
@codex-rs/core/tests/suite/compact.rs
@codex-rs/core/tests/suite/compact_remote.rs
@codex-rs/core/tests/suite/user_shell_cmd.rs
@codex-rs/app-server/tests/suite/v2/compaction.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add delayed local and remote auto-compaction continuation tests</name>
  <files>codex-rs/core/tests/suite/compact.rs, codex-rs/core/tests/suite/compact_remote.rs</files>
  <action>Use delayed mock responses so background compaction is provably still in flight while the main turn continues to emit more model progress. Add one local-path scenario and one remote-path scenario that assert the auto trigger happens after threshold crossing without any extra user input and that the active turn reaches continued follow-up work before compaction finishes.</action>
  <verify>cargo test -p codex-core compact</verify>
  <done>Core tests prove Phase 1 background compaction starts automatically and does not block continued turn progress for local or remote providers.</done>
</task>

<task type="auto">
  <name>Task 2: Add active-turn lifecycle and single-flight regression coverage</name>
  <files>codex-rs/core/tests/suite/compact.rs, codex-rs/core/tests/suite/compact_remote.rs, codex-rs/core/tests/suite/user_shell_cmd.rs</files>
  <action>Add assertions that background auto-compaction does not emit `TurnAborted(Replaced)` and does not create an extra turn lifecycle. Cover the Phase 1 single-flight rule by proving only one background auto-compaction starts per active turn. Add or adapt an interrupt path assertion showing the auxiliary compaction worker is cancelled when the active turn ends, using the existing auxiliary-operation test patterns instead of inventing a separate harness.</action>
  <verify>cargo test -p codex-core compact && cargo test -p codex-core user_shell_cmd</verify>
  <done>Regression tests fail if Phase 1 background compaction replaces the active turn, starts duplicate background jobs, or survives turn cancellation.</done>
</task>

<task type="auto">
  <name>Task 3: Update app-server compaction tests only for the Phase 1 surface</name>
  <files>codex-rs/app-server/tests/suite/v2/compaction.rs</files>
  <action>If the runtime still emits context-compaction item lifecycle notifications through the app-server surface, update those tests so they assert the new background ordering or continued-turn behavior without introducing replay, resume, transcript-splice, or fallback expectations. Keep this file focused on event-surface compatibility, not later-phase durability work.</action>
  <verify>cargo test -p codex-app-server compaction</verify>
  <done>App-server compaction coverage matches the Phase 1 runtime surface and stays scoped away from later-phase correctness concerns.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `just fix -p codex-core`
- [ ] If `codex-rs/app-server/tests/suite/v2/compaction.rs` requires app-server production-code changes, run `just fix -p codex-app-server`
- [ ] `just fmt`
- [ ] `cargo test -p codex-core compact`
- [ ] `cargo test -p codex-core user_shell_cmd`
- [ ] `cargo test -p codex-app-server compaction`
- [ ] Tests prove continued turn progress while background compaction is in flight and prove no replacement-task regression was introduced
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Core suites cover automatic start, continued progress, cancellation, and single-flight behavior for Phase 1 background compaction
- App-server coverage reflects the Phase 1 event surface only
- No tests in this plan assume transcript splicing, failure fallback, UI indicator behavior, or multi-compaction overlap
</success_criteria>

<output>
After completion, create `.planning/phases/01-background-trigger-and-continued-turns/01-04-SUMMARY.md`
</output>
