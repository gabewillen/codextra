---
phase: 01-background-trigger-and-continued-turns
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - codex-rs/core/src/compact_remote.rs
autonomous: true
requirements:
  - RUN-01
user_setup: []
must_haves:
  truths:
    - Remote auto-compaction can build a compaction result from a frozen history snapshot without mutating live session history while the worker is running.
    - Existing blocking remote compaction paths still apply the returned result through the current session-owned history replacement flow.
  artifacts:
    - codex-rs/core/src/compact_remote.rs
  key_links:
    - Snapshot-result helpers in codex-rs/core/src/compact_remote.rs remain callable from inline/manual paths now and from a detached background launcher in codex-rs/core/src/codex.rs later.
---

<objective>
Create the remote compaction worker/apply split needed for background mid-turn execution.

Purpose: The OpenAI/remote compaction path needs the same background-safe shape as the local path before `run_turn()` can launch it asynchronously.
Output: `codex-rs/core/src/compact_remote.rs` exposes snapshot-based result helpers while current inline callers keep their blocking behavior.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-background-trigger-and-continued-turns/01-RESEARCH.md
@.planning/phases/01-background-trigger-and-continued-turns/01-VALIDATION.md
@codex-rs/core/src/compact_remote.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract a background-safe remote compaction worker</name>
  <files>codex-rs/core/src/compact_remote.rs</files>
  <action>Refactor remote auto-compaction so the request execution, history trimming, remote-output processing, and failure logging can run against a frozen history snapshot and return a result object instead of mutating the live session immediately. Keep remote request semantics intact, but do not call `Session::replace_compacted_history`, `Session::recompute_token_usage`, or turn-item completion from the worker helper.</action>
  <verify>cargo test -p codex-core compact_remote</verify>
  <done>A result-producing helper exists for remote compaction, and its worker phase does not mutate session history while it is running.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild inline remote compaction on top of the worker result</name>
  <files>codex-rs/core/src/compact_remote.rs</files>
  <action>Update the existing inline remote compaction entry points to call the new worker helper and then apply the returned result synchronously. Preserve current manual and pre-turn blocking behavior, preserve compaction item lifecycle emission, and keep current replacement-history semantics intact so this plan stays out of transcript-splice correctness and failure-fallback work.</action>
  <verify>cargo test -p codex-core compact_remote</verify>
  <done>Current remote compaction callers still issue the existing compact request and apply their result synchronously, but the heavy work is now reusable from a background launcher.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `just fix -p codex-core`
- [ ] `just fmt`
- [ ] `cargo test -p codex-core compact_remote`
- [ ] Remote inline compaction still applies synchronously for manual and pre-turn paths while exposing a snapshot-result helper for later background use
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Remote compaction has a worker/apply split that Phase 1 runtime code can call without blocking the active turn
- No transcript-splice, fallback, indicator, or multi-compaction behavior is added in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/01-background-trigger-and-continued-turns/01-02-SUMMARY.md`
</output>
