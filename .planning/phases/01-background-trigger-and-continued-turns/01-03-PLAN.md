---
phase: 01-background-trigger-and-continued-turns
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - codex-rs/core/src/codex.rs
  - codex-rs/core/src/state/turn.rs
  - codex-rs/core/src/tasks/mod.rs
autonomous: true
requirements:
  - RUN-01
  - RUN-02
user_setup: []
must_haves:
  truths:
    - Mid-turn auto-compaction launches as auxiliary work under the active turn instead of replacing the turn.
    - Only one Phase 1 background auto-compaction can be in flight per active turn, and turn teardown cancels it.
    - The main turn loop continues to request and emit agent progress while background compaction runs.
    - Completed background compaction results are stored for later handling and are not applied to live history during Phase 1.
  artifacts:
    - codex-rs/core/src/codex.rs
    - codex-rs/core/src/state/turn.rs
    - codex-rs/core/src/tasks/mod.rs
  key_links:
    - `codex-rs/core/src/codex.rs` launches background auto-compaction by calling the snapshot-result helpers added in `codex-rs/core/src/compact.rs` and `codex-rs/core/src/compact_remote.rs`.
    - `codex-rs/core/src/state/turn.rs` tracks the auxiliary background job, and `codex-rs/core/src/tasks/mod.rs` clears it when the active turn finishes, is replaced, or is interrupted.
---

<objective>
Launch automatic mid-turn compaction in the background while keeping the active turn alive.

Purpose: This is the Phase 1 runtime behavior change that satisfies `RUN-01` and `RUN-02` without pulling transcript splice correctness or fallback behavior into scope.
Output: The active-turn runtime tracks one auxiliary background compaction job, starts it from `run_turn()`, keeps the main turn loop streaming while that job is in flight, and stores completed results without applying them to live history yet.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-background-trigger-and-continued-turns/01-RESEARCH.md
@.planning/phases/01-background-trigger-and-continued-turns/01-VALIDATION.md
@codex-rs/core/src/codex.rs
@codex-rs/core/src/state/turn.rs
@codex-rs/core/src/tasks/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add active-turn bookkeeping for one auxiliary auto-compaction job</name>
  <files>codex-rs/core/src/state/turn.rs, codex-rs/core/src/tasks/mod.rs</files>
  <action>Extend active-turn state with a single background auto-compaction slot that tracks the child cancellation token, join handle, snapshot marker, and completed worker result needed for Phase 1. Keep this separate from the regular task map so `spawn_task()` semantics remain unchanged. Update turn teardown so finishing, replacing, or interrupting the active turn cancels and clears the auxiliary compaction job and any stored result instead of leaking them.</action>
  <verify>cargo test -p codex-core user_shell_cmd</verify>
  <done>One active turn can own at most one background auto-compaction job, can retain at most one completed result for later phases, and normal task replacement semantics remain unchanged for real tasks.</done>
</task>

<task type="auto">
  <name>Task 2: Launch background compaction from `run_turn()` without blocking the loop</name>
  <files>codex-rs/core/src/codex.rs</files>
  <action>Change the post-sampling auto-compaction trigger so `run_turn()` snapshots `sess.clone_history().await`, registers a detached auxiliary async worker, and immediately continues the normal follow-up loop instead of awaiting compaction. Keep pre-turn compact and model-switch compact inline/blocking, suppress duplicate Phase 1 auto-trigger attempts while one background job is active, and do not route this work through `spawn_task()` because that would replace the active turn.</action>
  <verify>cargo test -p codex-core compact</verify>
  <done>Automatic mid-turn compaction starts without extra user action, does not replace the active turn, and does not block continued sampling.</done>
</task>

<task type="auto">
  <name>Task 3: Defer all background result application to Phase 2</name>
  <files>codex-rs/core/src/codex.rs, codex-rs/core/src/state/turn.rs, codex-rs/core/src/tasks/mod.rs</files>
  <action>When the background worker finishes, always store the completed result on turn-owned state and never apply it to live session history in Phase 1. The active turn may continue emitting new output, and Phase 1 explicitly avoids proving splice correctness, so result application is deferred wholesale to Phase 2. Cancel outstanding background work when the turn ends. Do not add fallback-on-failure logic, transcript splice correctness, UI indicator state, or multi-compaction overlap in this plan.</action>
  <verify>cargo test -p codex-core compact</verify>
  <done>Finishing background compaction never replaces the active turn and never mutates live history during Phase 1; completed results are only stored for later handling.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `just fix -p codex-core`
- [ ] `just fmt`
- [ ] `cargo test -p codex-core compact`
- [ ] `cargo test -p codex-core user_shell_cmd`
- [ ] Automatic mid-turn compaction runs as auxiliary work while the active turn continues streaming progress
- [ ] Completed background results are retained without any live-history apply path in this phase
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `run_turn()` launches background auto-compaction without blocking continued agent progress
- Active-turn teardown cancels the auxiliary worker cleanly
- Phase 1 stores completed background compaction results without applying them to live history
- Phase 1 still avoids transcript splicing, fallback-on-failure, UI indicator work, and multi-compaction overlap
</success_criteria>

<output>
After completion, create `.planning/phases/01-background-trigger-and-continued-turns/01-03-SUMMARY.md`
</output>
