---
phase: 01-background-trigger-and-continued-turns
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - codex-rs/core/src/compact.rs
autonomous: true
requirements:
  - RUN-01
user_setup: []
must_haves:
  truths:
    - Local auto-compaction can build a compaction result from a frozen history snapshot without mutating live session history while the worker is running.
    - Existing blocking local compaction paths still apply the returned result through the current session-owned history replacement flow.
  artifacts:
    - codex-rs/core/src/compact.rs
  key_links:
    - Snapshot-result helpers in codex-rs/core/src/compact.rs remain callable from inline/manual paths now and from a detached background launcher in codex-rs/core/src/codex.rs later.
---

<objective>
Create the local compaction worker/apply split needed for background mid-turn execution.

Purpose: Phase 1 needs a background-safe local compaction path before `run_turn()` can launch compaction without blocking.
Output: `codex-rs/core/src/compact.rs` exposes snapshot-based result helpers while current inline callers keep their blocking behavior.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-background-trigger-and-continued-turns/01-RESEARCH.md
@.planning/phases/01-background-trigger-and-continued-turns/01-VALIDATION.md
@codex-rs/core/src/compact.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract a background-safe local compaction worker</name>
  <files>codex-rs/core/src/compact.rs</files>
  <action>Refactor the local auto-compaction implementation so the request/summary building path can run against a frozen history snapshot and return a result object instead of mutating the live session immediately. Keep retry logic, context-window trimming, summary construction, and initial-context injection rules inside the worker helper, but do not call `Session::replace_compacted_history`, `Session::recompute_token_usage`, or turn-item completion from that helper.</action>
  <verify>cargo test -p codex-core compact</verify>
  <done>A result-producing helper exists for local compaction, and its worker phase does not mutate session history while it is running.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild inline local compaction on top of the worker result</name>
  <files>codex-rs/core/src/compact.rs</files>
  <action>Update the existing inline local compaction entry points to call the new worker helper and then apply the returned result synchronously. Preserve the current blocking behavior for manual and pre-turn compaction, preserve warning emission, and keep the current replacement-history semantics intact so this plan does not pull transcript-splice correctness into Phase 1.</action>
  <verify>cargo test -p codex-core compact</verify>
  <done>Current local compaction callers still emit the expected lifecycle and warning events, but the heavy work is now reusable from a background launcher.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `just fix -p codex-core`
- [ ] `just fmt`
- [ ] `cargo test -p codex-core compact`
- [ ] Local inline compaction still applies synchronously for manual and pre-turn paths while exposing a snapshot-result helper for later background use
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Local compaction has a worker/apply split that Phase 1 runtime code can call without blocking the active turn
- No transcript-splice, fallback, indicator, or multi-compaction behavior is added in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/01-background-trigger-and-continued-turns/01-01-SUMMARY.md`
</output>
