---
phase: 02-safe-transcript-splicing
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - codex-rs/core/src/codex_tests.rs
  - codex-rs/core/tests/suite/compact.rs
  - codex-rs/core/tests/suite/compact_remote.rs
autonomous: true
requirements:
  - HIST-01
  - HIST-02
user_setup: []
must_haves:
  truths:
    - Tests prove only the captured transcript prefix is replaced when background compaction finishes.
    - Tests prove messages created while compaction was running remain below the compacted top section in the correct order.
    - Tests fail if the splice duplicates, reorders, or drops newer history.
  artifacts:
    - codex-rs/core/src/codex_tests.rs
    - codex-rs/core/tests/suite/compact.rs
    - codex-rs/core/tests/suite/compact_remote.rs
  key_links:
    - `codex-rs/core/src/codex_tests.rs` covers pure splice and turn-owned apply invariants.
    - `codex-rs/core/tests/suite/compact.rs` and `codex-rs/core/tests/suite/compact_remote.rs` prove local and remote live transcript order after background compaction completion.
---

<objective>
Lock Phase 2 transcript-splice behavior behind direct regression coverage.

Purpose: Phase 2 changes live transcript mutation semantics, so the phase must end with tests that prove only the intended prefix is replaced and newer messages stay below it in order.
Output: helper-level and end-to-end core tests that fail on duplicated, reordered, or dropped history after background compaction applies.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-safe-transcript-splicing/02-RESEARCH.md
@.planning/phases/02-safe-transcript-splicing/02-VALIDATION.md
@.planning/phases/02-safe-transcript-splicing/02-02-PLAN.md
@codex-rs/core/src/codex_tests.rs
@codex-rs/core/tests/suite/compact.rs
@codex-rs/core/tests/suite/compact_remote.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add splice helper and single-apply invariants</name>
  <files>codex-rs/core/src/codex_tests.rs</files>
  <action>Add direct tests for the prefix-splice helper and the turn-owned apply path. Cover exact-prefix replacement, preserved appended tail order, mismatch guard behavior, and single-apply clearing semantics.</action>
  <verify>cargo test -p codex-core compact</verify>
  <done>Pure and turn-owned application logic fail fast if the splice would replace the wrong range or apply more than once.</done>
</task>

<task type="auto">
  <name>Task 2: Add local and remote live transcript ordering regressions</name>
  <files>codex-rs/core/tests/suite/compact.rs, codex-rs/core/tests/suite/compact_remote.rs</files>
  <action>Update the successful background compaction regressions so they assert the live continuation history after apply, not merely the request shape before apply. Cover both local and remote providers and fail on duplicated summaries, reordered tail items, or dropped in-flight messages.</action>
  <verify>cargo test -p codex-core compact</verify>
  <done>Integration tests prove background compaction rewrites only the intended prefix and preserves newer appended items below it in order.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p codex-core compact`
- [ ] Tests fail on duplicated, reordered, or dropped tail items
- [ ] Local and remote paths both assert post-apply transcript order
- [ ] Helper-level tests guard prefix mismatch and single-apply behavior
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 2 has automated proof that only the intended transcript prefix is replaced
- Phase 2 has automated proof that newer messages remain below the compacted top section in order
</success_criteria>

<output>
After completion, create `.planning/phases/02-safe-transcript-splicing/02-03-SUMMARY.md`
</output>
