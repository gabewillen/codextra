---
phase: 02-safe-transcript-splicing
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - codex-rs/core/src/codex.rs
  - codex-rs/core/src/state/turn.rs
  - codex-rs/core/src/compact.rs
  - codex-rs/core/src/compact_remote.rs
autonomous: true
requirements:
  - HIST-01
  - HIST-02
user_setup: []
must_haves:
  truths:
    - Completed background auto-compaction results are applied exactly once through the turn-owned runtime path.
    - Applying a completed result replaces only the captured snapshot prefix and preserves newer appended items below it in order.
    - After successful apply, the stored completed background result is cleared so it cannot reapply or block later work incorrectly.
  artifacts:
    - codex-rs/core/src/codex.rs
    - codex-rs/core/src/state/turn.rs
    - codex-rs/core/src/compact.rs
    - codex-rs/core/src/compact_remote.rs
  key_links:
    - `codex-rs/core/src/codex.rs` polls for completed background results and applies them on the main turn runtime path.
    - `codex-rs/core/src/compact.rs` and `codex-rs/core/src/compact_remote.rs` continue to supply the replacement history that Phase 2 splices into the live transcript.
---

<objective>
Apply completed background compactions to live history without losing or reordering newer messages.

Purpose: This is the Phase 2 behavior change that turns Phase 1's stored background results into safe live transcript updates.
Output: the active turn applies a completed background compaction through the existing compaction history replacement path, but only after rebuilding live history as `replacement_history + preserved_tail`.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-safe-transcript-splicing/02-RESEARCH.md
@.planning/phases/02-safe-transcript-splicing/02-VALIDATION.md
@.planning/phases/02-safe-transcript-splicing/02-01-PLAN.md
@codex-rs/core/src/codex.rs
@codex-rs/core/src/state/turn.rs
@codex-rs/core/src/compact.rs
@codex-rs/core/src/compact_remote.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire completed background results into the active-turn runtime</name>
  <files>codex-rs/core/src/codex.rs, codex-rs/core/src/state/turn.rs</files>
  <action>Add a turn-owned apply point that checks for a completed background auto-compaction result at safe points in `run_turn()`, applies it once if the snapshot prefix still matches, and clears the stored outcome. Keep background result application off the detached worker task.</action>
  <verify>cargo test -p codex-core compact</verify>
  <done>Completed results can be applied during an active turn without racing the detached worker and without repeating after success.</done>
</task>

<task type="auto">
  <name>Task 2: Reuse the existing compaction replacement path for local and remote outcomes</name>
  <files>codex-rs/core/src/codex.rs, codex-rs/core/src/compact.rs, codex-rs/core/src/compact_remote.rs</files>
  <action>Convert the stored local and remote background outcomes into a common apply shape that uses the existing `replace_compacted_history(...)` path after rebuilding the correct spliced history. Recompute token usage and preserve `reference_context_item` semantics.</action>
  <verify>cargo test -p codex-core compact</verify>
  <done>Successful local and remote background compactions update the live transcript through one shared apply path that preserves appended tail items.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p codex-core compact`
- [ ] Completed background results apply exactly once
- [ ] Applying a result preserves appended tail order and content
- [ ] Local and remote paths share the same live-history apply contract
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Live history is spliced safely when a completed background result is ready
- Stored completed results are cleared after apply or explicit mismatch handling
- Phase 2 still avoids fallback, indicator UI, and multi-compaction overlap work
</success_criteria>

<output>
After completion, create `.planning/phases/02-safe-transcript-splicing/02-02-SUMMARY.md`
</output>
