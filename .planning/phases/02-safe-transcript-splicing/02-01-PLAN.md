---
phase: 02-safe-transcript-splicing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - codex-rs/core/src/state/turn.rs
  - codex-rs/core/src/context_manager/history.rs
  - codex-rs/core/src/codex.rs
autonomous: true
requirements:
  - HIST-01
user_setup: []
must_haves:
  truths:
    - Completed background auto-compaction outcomes retain enough snapshot metadata to identify exactly which live-history prefix they are allowed to replace.
    - A pure helper can replace a captured history prefix while preserving appended tail items in order.
    - Prefix mismatch is detectable before any live-history mutation occurs.
  artifacts:
    - codex-rs/core/src/state/turn.rs
    - codex-rs/core/src/context_manager/history.rs
    - codex-rs/core/src/codex.rs
  key_links:
    - `codex-rs/core/src/state/turn.rs` stores the completed background result plus the captured snapshot metadata needed for safe apply.
    - `codex-rs/core/src/context_manager/history.rs` owns the pure prefix-splice helper that Phase 2 runtime code will call.
---

<objective>
Prepare the data and pure splice primitive needed to apply completed background compactions safely.

Purpose: Phase 2 cannot safely mutate live history until the completed background result knows exactly what snapshot it was built from and there is a deterministic helper for replacing only that captured prefix.
Output: stored background results retain snapshot metadata, and a pure helper can rebuild `replacement_history + appended_tail` while rejecting mismatched prefixes.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-safe-transcript-splicing/02-RESEARCH.md
@.planning/phases/02-safe-transcript-splicing/02-VALIDATION.md
@codex-rs/core/src/codex.rs
@codex-rs/core/src/state/turn.rs
@codex-rs/core/src/context_manager/history.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend stored background outcomes with snapshot splice metadata</name>
  <files>codex-rs/core/src/state/turn.rs, codex-rs/core/src/codex.rs</files>
  <action>Retain the captured history snapshot or equivalent exact prefix metadata alongside each completed background auto-compaction result. Keep the Phase 1 single-flight rule intact and do not apply anything yet in this task.</action>
  <verify>cargo test -p codex-core compact</verify>
  <done>Completed background outcomes carry enough data to prove which prefix of live history they are allowed to replace.</done>
</task>

<task type="auto">
  <name>Task 2: Add a pure prefix-splice helper for compaction application</name>
  <files>codex-rs/core/src/context_manager/history.rs</files>
  <action>Add a deterministic helper that takes the captured snapshot, the current live history, and the completed replacement history, then returns the spliced history only when the live history still begins with the captured snapshot. Preserve appended tail items in order and leave mismatch handling explicit.</action>
  <verify>cargo test -p codex-core compact</verify>
  <done>Phase 2 has a reusable, testable primitive for replacing only the captured transcript prefix while preserving newer items.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p codex-core compact`
- [ ] Stored outcomes include exact splice metadata
- [ ] Prefix-splice helper preserves appended tail ordering
- [ ] Prefix mismatch is detectable without mutating live history
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Completed background results know which live-history prefix they can replace
- Phase 2 has a pure, reusable splice helper before any runtime apply work lands
</success_criteria>

<output>
After completion, create `.planning/phases/02-safe-transcript-splicing/02-01-SUMMARY.md`
</output>
