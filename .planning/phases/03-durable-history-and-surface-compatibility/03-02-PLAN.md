---
phase: 03-durable-history-and-surface-compatibility
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - codex-rs/app-server-protocol/src/protocol/thread_history.rs
  - codex-rs/app-server/src/codex_message_processor.rs
  - codex-rs/app-server/README.md
autonomous: true
requirements:
  - HIST-03
  - COMP-03
user_setup: []
must_haves:
  truths:
    - App-server thread/read and resume surfaces remain compatible with persisted spliced compaction history.
    - Thread-history reconstruction keeps compaction-only and rollback-affected turns coherent when replacement history is present.
    - Existing compaction-related thread-item and notification flows remain compatible with the durable background compaction rollout shape.
    - Any user-visible app-server thread-history semantics changed by Phase 3 are documented.
  artifacts:
    - codex-rs/app-server-protocol/src/protocol/thread_history.rs
    - codex-rs/app-server/src/codex_message_processor.rs
    - codex-rs/app-server/README.md
  key_links:
    - `codex-rs/app-server-protocol/src/protocol/thread_history.rs` converts rollout items into app-server turns and currently treats `RolloutItem::Compacted` specially.
    - `codex-rs/app-server/src/codex_message_processor.rs` feeds thread/read and resume surfaces from the protocol thread-history builder.
---

<objective>
Keep app-server history surfaces compatible with persisted Phase 2 compaction semantics.

Purpose: Even if core replay is correct, users still lose compatibility if app-server thread/read and resume surfaces expose a different thread shape after compaction. This plan aligns those surfaces with the durable history contract.
Output: app-server thread-history consumers remain coherent when persisted rollout data contains spliced compaction replacement histories and rollback markers.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-durable-history-and-surface-compatibility/03-RESEARCH.md
@.planning/phases/03-durable-history-and-surface-compatibility/03-VALIDATION.md
@.planning/phases/03-durable-history-and-surface-compatibility/03-01-PLAN.md
@codex-rs/app-server-protocol/src/protocol/thread_history.rs
@codex-rs/app-server/src/codex_message_processor.rs
@codex-rs/app-server/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align thread-history reconstruction with persisted compaction data</name>
  <files>codex-rs/app-server-protocol/src/protocol/thread_history.rs, codex-rs/app-server/src/codex_message_processor.rs</files>
  <action>Update the app-server thread-history/replay path so persisted compaction markers and replacement-history data remain compatible with thread/read, thread/resume, rollback-backed surfaces, and existing thread-item/compaction-notification consumers. Preserve compaction-only turn handling and backward compatibility for older rollout data.</action>
  <verify>cargo test -p codex-app-server-protocol thread_history</verify>
  <done>App-server thread-history consumers do not drift from the persisted post-compaction transcript contract established by core replay.</done>
</task>

<task type="auto">
  <name>Task 2: Document any app-server surface semantics that become explicit in Phase 3</name>
  <files>codex-rs/app-server/README.md</files>
  <action>Update app-server documentation if Phase 3 makes thread/read, thread/resume, or compaction-history behavior more explicit or changes what clients should expect around compaction-only turns and replayed history.</action>
  <verify>cargo test -p codex-app-server compaction && cargo test -p codex-app-server thread_rollback</verify>
  <done>App-server docs and runtime behavior describe the same compaction-history compatibility contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p codex-app-server-protocol thread_history`
- [ ] `cargo test -p codex-app-server compaction`
- [ ] `cargo test -p codex-app-server thread_rollback`
- [ ] Thread/read and resume surfaces remain compatible with persisted spliced compaction history
- [ ] Existing compaction-related thread-item and notification flows remain compatible
- [ ] Backward compatibility with older compaction rollouts remains intact
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- App-server thread-history/read consumers remain compatible with Phase 2 compaction persistence
- Existing compaction-related thread-item and notification flows remain compatible with durable background compaction replay
- Any user-visible app-server semantics clarified by the phase are documented
</success_criteria>

<output>
After completion, create `.planning/phases/03-durable-history-and-surface-compatibility/03-02-SUMMARY.md`
</output>
