---
phase: 03-durable-history-and-surface-compatibility
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - codex-rs/core/src/codex_tests.rs
  - codex-rs/app-server-protocol/src/protocol/thread_history.rs
  - codex-rs/app-server/tests/suite/v2/compaction.rs
  - codex-rs/app-server/tests/suite/v2/thread_rollback.rs
autonomous: true
requirements:
  - HIST-03
  - COMP-03
user_setup: []
must_haves:
  truths:
    - Automated tests prove live, resume, rollback, and app-server read surfaces stay aligned after persisted Phase 2 compaction splices.
    - Tests cover both new spliced replacement-history rollouts and legacy compaction rollouts without replacement history.
    - Tests prove existing compaction-related thread-item and notification flows remain compatible with durable background compaction replay.
    - Cross-surface regressions fail if a consumer drops, reorders, or silently hides the persisted post-compaction transcript shape unexpectedly.
  artifacts:
    - codex-rs/core/src/codex_tests.rs
    - codex-rs/app-server-protocol/src/protocol/thread_history.rs
    - codex-rs/app-server/tests/suite/v2/compaction.rs
    - codex-rs/app-server/tests/suite/v2/thread_rollback.rs
  key_links:
    - `codex-rs/core/src/codex_tests.rs` is the core parity harness for replay/resume/rollback reconstruction.
    - `codex-rs/app-server/tests/suite/v2/compaction.rs` and `codex-rs/app-server/tests/suite/v2/thread_rollback.rs` are the app-server compatibility harnesses for read/resume/rollback surfaces.
---

<objective>
Lock durable compaction compatibility behind direct core and app-server regression coverage.

Purpose: Phase 3 changes how persisted compaction state is interpreted across multiple surfaces. The phase should end with automated proof that those surfaces stay aligned for both new and legacy rollout data.
Output: core and app-server regression coverage that fails on replay/read compatibility mismatches after persisted compaction.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-durable-history-and-surface-compatibility/03-RESEARCH.md
@.planning/phases/03-durable-history-and-surface-compatibility/03-VALIDATION.md
@.planning/phases/03-durable-history-and-surface-compatibility/03-01-PLAN.md
@.planning/phases/03-durable-history-and-surface-compatibility/03-02-PLAN.md
@codex-rs/core/src/codex_tests.rs
@codex-rs/app-server-protocol/src/protocol/thread_history.rs
@codex-rs/app-server/tests/suite/v2/compaction.rs
@codex-rs/app-server/tests/suite/v2/thread_rollback.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add core parity regressions for persisted spliced compaction history</name>
  <files>codex-rs/core/src/codex_tests.rs</files>
  <action>Add direct tests that compare live Phase 2 post-compaction history with resumed, forked, and rollback-replayed history when `CompactedItem.replacement_history` is populated. Keep legacy compaction coverage in place.</action>
  <verify>cargo test -p codex-core compact</verify>
  <done>Core parity tests fail if persisted spliced compaction history reconstructs differently from the live transcript.</done>
</task>

<task type="auto">
  <name>Task 2: Add app-server compatibility regressions for compaction read and rollback flows</name>
  <files>codex-rs/app-server-protocol/src/protocol/thread_history.rs, codex-rs/app-server/tests/suite/v2/compaction.rs, codex-rs/app-server/tests/suite/v2/thread_rollback.rs</files>
  <action>Add protocol-level and end-to-end tests for app-server thread history so compaction-only turns, persisted replacement history, rollback markers, and existing compaction-related thread-item/notification flows all stay compatible across thread/read and related surfaces.</action>
  <verify>cargo test -p codex-app-server-protocol thread_history && cargo test -p codex-app-server compaction && cargo test -p codex-app-server thread_rollback</verify>
  <done>App-server regressions fail when compaction compatibility drifts across read, resume, or rollback surfaces.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p codex-core compact`
- [ ] `cargo test -p codex-app-server-protocol thread_history`
- [ ] `cargo test -p codex-app-server compaction`
- [ ] `cargo test -p codex-app-server thread_rollback`
- [ ] Core replay and app-server read/rollback surfaces agree on persisted compaction compatibility
- [ ] Existing compaction-related thread-item and notification flows remain covered
- [ ] Legacy and new compaction rollout shapes are both covered
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 3 has automated proof of durable post-compaction parity across the supported surfaces
- Phase 3 has automated proof that existing compaction-related thread-item and notification flows remain compatible
- Phase 3 leaves failure fallback and visibility work for later phases without diluting compatibility guarantees
</success_criteria>

<output>
After completion, create `.planning/phases/03-durable-history-and-surface-compatibility/03-03-SUMMARY.md`
</output>
