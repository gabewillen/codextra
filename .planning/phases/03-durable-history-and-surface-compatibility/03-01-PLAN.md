---
phase: 03-durable-history-and-surface-compatibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - codex-rs/core/src/codex.rs
  - codex-rs/core/src/codex_tests.rs
autonomous: true
requirements:
  - HIST-03
user_setup: []
must_haves:
  truths:
    - Persisted spliced compaction replacement histories replay into the same history shape that live Phase 2 users saw.
    - Resume and fork startup rebuild reference-context and previous-turn state consistently after a persisted background compaction apply.
    - Rollback replay continues to work when the rollout already contains spliced compaction replacement history.
  artifacts:
    - codex-rs/core/src/codex.rs
    - codex-rs/core/src/codex_tests.rs
  key_links:
    - `codex-rs/core/src/codex.rs` owns `record_initial_history(...)`, rollback replay, and `reconstruct_history_from_rollout(...)` consumers.
    - `codex-rs/core/src/codex_tests.rs` already covers replay, resume, fork, and rollback seams that Phase 3 must extend.
---

<objective>
Make the core replay/resume/rollback path durable for Phase 2's spliced compaction history.

Purpose: Phase 2 changed persisted compaction data from prefix-only semantics to fully spliced replacement histories. Phase 3 must ensure core reconstruction and rollback replay materialize the same transcript and baseline state users saw live.
Output: core history reconstruction, resume/fork startup, and rollback replay all remain consistent when a rollout contains persisted spliced compaction results.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-durable-history-and-surface-compatibility/03-RESEARCH.md
@.planning/phases/03-durable-history-and-surface-compatibility/03-VALIDATION.md
@codex-rs/core/src/codex.rs
@codex-rs/core/src/codex_tests.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align core reconstruction with persisted spliced compaction history</name>
  <files>codex-rs/core/src/codex.rs</files>
  <action>Audit and update the reconstruction paths used by `record_initial_history(...)` and rollback replay so persisted `CompactedItem.replacement_history` from Phase 2 is treated as the source of truth for reconstructed history while legacy compaction entries without replacement history remain supported.</action>
  <verify>cargo test -p codex-core compact</verify>
  <done>Resume, fork, and rollback reconstruction use the persisted post-compaction transcript shape instead of drifting back toward older prefix-only semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve reconstructed baseline state after persisted compaction apply</name>
  <files>codex-rs/core/src/codex.rs</files>
  <action>Ensure resumed and rollback-replayed sessions re-establish `reference_context_item`, previous-turn settings, and token/accounting state consistently after replaying a persisted spliced compaction result.</action>
  <verify>cargo test -p codex-core compact</verify>
  <done>Core replay restores both transcript content and the baseline state later turns depend on.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p codex-core compact`
- [ ] Resume/fork replay reconstructs the same post-compaction transcript as live Phase 2 apply
- [ ] Rollback replay remains consistent after persisted spliced compactions
- [ ] Legacy compaction entries without replacement history still reconstruct safely
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Core replay/readback paths are durable for persisted Phase 2 splice semantics
- Baseline state after resume/rollback matches the reconstructed transcript shape
</success_criteria>

<output>
After completion, create `.planning/phases/03-durable-history-and-surface-compatibility/03-01-SUMMARY.md`
</output>
