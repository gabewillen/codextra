---
phase: 05-visible-rolling-background-compaction
plan: 04
type: execute
wave: 3
depends_on:
  - 02
  - 03
files_modified:
  - codex-rs/core/tests/suite/compact.rs
  - codex-rs/core/tests/suite/compact_remote.rs
  - codex-rs/tui/src/chatwidget/tests.rs
  - codex-rs/app-server/tests/suite/v2/compaction.rs
  - codex-rs/app-server/README.md
autonomous: true
requirements:
  - RUN-03
  - VIS-01
  - VIS-02
user_setup: []
must_haves:
  truths:
    - Rolling background compaction is covered end to end across local core, remote core, TUI, and app-server surfaces.
    - TUI snapshot coverage locks the below-input indicator and no-chatter behavior so future transcript regressions are obvious.
    - App-server compatibility and docs remain aligned with the final compaction lifecycle contract.
  artifacts:
    - codex-rs/core/tests/suite/compact.rs
    - codex-rs/core/tests/suite/compact_remote.rs
    - codex-rs/tui/src/chatwidget/tests.rs
    - codex-rs/app-server/tests/suite/v2/compaction.rs
    - codex-rs/app-server/README.md
  key_links:
    - Phase 5 changes span runtime, UI, and server surfaces, so final regression proof has to cross crates.
    - `codex-rs/app-server/tests/suite/v2/compaction.rs` is already the compatibility home for compaction lifecycle notifications.
    - Any changed deprecated compaction behavior needs to be reflected in `codex-rs/app-server/README.md`.
---

<objective>
Lock Phase 5 behind cross-surface regressions and any required compaction docs updates.

Purpose: rolling background compaction touches core orchestration, footer UX, and app-server notifications. This final plan proves the feature works end to end and leaves the documented compatibility surface in sync with the implementation.
Output: final regression coverage proves rolling overlap and the below-input indicator work together, and any changed compaction notification semantics are documented.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-visible-rolling-background-compaction/05-RESEARCH.md
@.planning/phases/05-visible-rolling-background-compaction/05-VALIDATION.md
@codex-rs/core/tests/suite/compact.rs
@codex-rs/core/tests/suite/compact_remote.rs
@codex-rs/tui/src/chatwidget/tests.rs
@codex-rs/app-server/tests/suite/v2/compaction.rs
@codex-rs/app-server/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add final rolling-compaction regression coverage across local, remote, and TUI surfaces</name>
  <files>codex-rs/core/tests/suite/compact.rs, codex-rs/core/tests/suite/compact_remote.rs, codex-rs/tui/src/chatwidget/tests.rs</files>
  <action>Extend final regression suites so rolling overlap, same-turn tail preservation, failure cleanup, below-input indicator rendering, and no-chatter behavior are all locked in together.</action>
  <verify>cargo test -p codex-core compact && cargo test -p codex-tui</verify>
  <done>Phase 5 behavior is covered where future regressions are most likely to appear.</done>
</task>

<task type="auto">
  <name>Task 2: Align app-server compatibility tests and docs with the final compaction lifecycle</name>
  <files>codex-rs/app-server/tests/suite/v2/compaction.rs, codex-rs/app-server/README.md</files>
  <action>Update app-server compaction tests and README guidance so notification ordering, durable compaction markers, and any changed deprecated compaction semantics stay synchronized with the final rolling implementation.</action>
  <verify>cargo test -p codex-app-server compaction</verify>
  <done>App-server surfaces remain documented and tested after rolling background compaction lands.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p codex-core compact && cargo test -p codex-tui`
- [ ] `cargo test -p codex-app-server compaction`
- [ ] Final regressions cover rolling overlap, failure cleanup, footer indicator, and no-chatter behavior
- [ ] App-server compatibility tests still pass with the final lifecycle contract
- [ ] `codex-rs/app-server/README.md` reflects any intentionally changed compaction notification semantics
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 5 has durable proof across runtime, UI, and server consumers
- Documentation and tests now describe the same compaction behavior
</success_criteria>

<output>
After completion, create `.planning/phases/05-visible-rolling-background-compaction/05-04-SUMMARY.md`
</output>
