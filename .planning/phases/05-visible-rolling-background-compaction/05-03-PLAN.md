---
phase: 05-visible-rolling-background-compaction
plan: 03
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - codex-rs/tui/src/chatwidget.rs
  - codex-rs/tui/src/chatwidget/tests.rs
  - codex-rs/tui/src/bottom_pane/mod.rs
  - codex-rs/tui/src/bottom_pane/chat_composer.rs
  - codex-rs/tui/src/bottom_pane/footer.rs
autonomous: true
requirements:
  - VIS-01
  - VIS-02
user_setup: []
must_haves:
  truths:
    - Background compaction activity is visible in a lightweight indicator below the input instead of as transcript chatter.
    - The indicator is driven by compaction lifecycle state and remains accurate when multiple background compactions overlap.
    - Successful background auto-compactions no longer render `Context compacted` transcript messages in the TUI.
  artifacts:
    - codex-rs/tui/src/chatwidget.rs
    - codex-rs/tui/src/chatwidget/tests.rs
    - codex-rs/tui/src/bottom_pane/mod.rs
    - codex-rs/tui/src/bottom_pane/chat_composer.rs
    - codex-rs/tui/src/bottom_pane/footer.rs
  key_links:
    - `codex-rs/tui/src/bottom_pane/mod.rs` currently renders the generic busy row above the composer, not below it.
    - `codex-rs/tui/src/bottom_pane/chat_composer.rs` already owns footer rows beneath the input and is the right placement for this indicator.
    - `codex-rs/tui/src/chatwidget.rs` still turns `EventMsg::ContextCompacted(_)` into the visible transcript message `Context compacted`.
---

<objective>
Add a below-input rolling compaction indicator and remove successful background compaction chatter from the transcript.

Purpose: the user asked for non-stop visible agent interaction with background compaction moved out of the transcript. This plan makes compaction visible in the right place and stops successful background compactions from reading like interruptions.
Output: the TUI shows a lightweight compaction indicator below the input while background compactions are active, and successful background compactions no longer inject transcript chatter.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-visible-rolling-background-compaction/05-RESEARCH.md
@.planning/phases/05-visible-rolling-background-compaction/05-VALIDATION.md
@codex-rs/tui/src/chatwidget.rs
@codex-rs/tui/src/chatwidget/tests.rs
@codex-rs/tui/src/bottom_pane/mod.rs
@codex-rs/tui/src/bottom_pane/chat_composer.rs
@codex-rs/tui/src/bottom_pane/footer.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Track rolling background compaction activity in the TUI</name>
  <files>codex-rs/tui/src/chatwidget.rs, codex-rs/tui/src/bottom_pane/mod.rs</files>
  <action>Teach the chat widget and bottom pane to maintain a compaction-specific activity state from compaction lifecycle events so the UI can render a dedicated indicator without reusing the generic task-running transcript status row.</action>
  <verify>cargo test -p codex-tui</verify>
  <done>The TUI has an explicit rolling compaction activity model rather than inferring compaction from generic busy state alone.</done>
</task>

<task type="auto">
  <name>Task 2: Render a lightweight below-input indicator and suppress transcript chatter</name>
  <files>codex-rs/tui/src/bottom_pane/chat_composer.rs, codex-rs/tui/src/bottom_pane/footer.rs, codex-rs/tui/src/chatwidget.rs, codex-rs/tui/src/chatwidget/tests.rs</files>
  <action>Add the compaction indicator to the composer/footer surface beneath the input and update compaction event handling so successful background auto-compactions stop rendering `Context compacted` as transcript output.</action>
  <verify>cargo test -p codex-tui && cargo insta pending-snapshots -p codex-tui</verify>
  <done>Users can see background compactions below the input while successful background compactions stop reading like interruptions in the transcript.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p codex-tui`
- [ ] `cargo insta pending-snapshots -p codex-tui`
- [ ] A lightweight compaction indicator renders below the input while background compaction is active
- [ ] Successful background compactions no longer render `Context compacted` transcript chatter
- [ ] Indicator state can represent more than one active background compaction
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The visible compaction signal now matches the user's requested placement and tone
- This plan leaves manual/failure behavior explicit rather than accidentally hiding all compaction feedback
</success_criteria>

<output>
After completion, create `.planning/phases/05-visible-rolling-background-compaction/05-03-SUMMARY.md`
</output>
