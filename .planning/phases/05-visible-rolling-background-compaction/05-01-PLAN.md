---
phase: 05-visible-rolling-background-compaction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - codex-rs/core/src/state/turn.rs
  - codex-rs/core/src/codex.rs
  - codex-rs/core/src/codex_tests.rs
autonomous: true
requirements:
  - RUN-03
user_setup: []
must_haves:
  truths:
    - Active turns can track multiple background auto-compactions concurrently instead of a single in-flight/completed slot.
    - New background auto-compactions only launch for a newer captured transcript range, not as duplicates of an already tracked snapshot.
    - Every tracked background job keeps enough metadata to support later ordered apply, stale-drop, or failure recovery decisions.
  artifacts:
    - codex-rs/core/src/state/turn.rs
    - codex-rs/core/src/codex.rs
    - codex-rs/core/src/codex_tests.rs
  key_links:
    - `codex-rs/core/src/state/turn.rs` currently hard-codes one `background_auto_compaction` and one `completed_background_auto_compaction`.
    - `codex-rs/core/src/codex.rs` owns launch timing in `start_background_auto_compact(...)` and is where rolling eligibility belongs.
    - Phase 5 requires overlap on different transcript ranges, so launch metadata must encode more than the current snapshot id alone.
---

<objective>
Replace the single-flight background auto-compaction state with rolling tracked jobs that can overlap on newer transcript ranges.

Purpose: Phase 4 made single background failures recover correctly, but the active-turn state machine still blocks overlap entirely. Phase 5 starts by giving core a job registry and distinct-range launch rules so later plans can safely apply and surface multiple background compactions.
Output: background auto-compaction state supports more than one tracked job per turn, and the launch path only starts a new job when the captured transcript range is newer than the ranges already being tracked.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-visible-rolling-background-compaction/05-RESEARCH.md
@.planning/phases/05-visible-rolling-background-compaction/05-VALIDATION.md
@codex-rs/core/src/state/turn.rs
@codex-rs/core/src/codex.rs
@codex-rs/core/src/codex_tests.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace single-slot background compaction tracking with rolling job metadata</name>
  <files>codex-rs/core/src/state/turn.rs, codex-rs/core/src/codex_tests.rs</files>
  <action>Introduce turn-owned tracking for multiple in-flight and settled background auto-compaction jobs, including the captured-range metadata needed to distinguish newer snapshots from duplicates and to consume terminal outcomes later in deterministic order.</action>
  <verify>cargo test -p codex-core compact</verify>
  <done>Active-turn state no longer assumes there can be only one background auto-compaction at a time.</done>
</task>

<task type="auto">
  <name>Task 2: Gate background compaction launches to newer captured ranges</name>
  <files>codex-rs/core/src/codex.rs, codex-rs/core/src/codex_tests.rs</files>
  <action>Update the post-sampling launch path so rolling background compaction starts only when the current captured range advances beyond the newest relevant tracked job, avoiding duplicate work against the same snapshot.</action>
  <verify>cargo test -p codex-core compact</verify>
  <done>Core can launch overlapping background compactions without repeatedly re-running the same snapshot range.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p codex-core compact`
- [ ] More than one background auto-compaction can be tracked at once for the same active turn
- [ ] Duplicate same-range launches are rejected or skipped deterministically
- [ ] Launch metadata is sufficient for later apply/recovery ordering decisions
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Core now has a rolling compaction foundation for `RUN-03`
- This plan stays focused on state and launch eligibility, not UI or final apply behavior
</success_criteria>

<output>
After completion, create `.planning/phases/05-visible-rolling-background-compaction/05-01-SUMMARY.md`
</output>
