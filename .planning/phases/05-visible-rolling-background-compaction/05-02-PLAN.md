---
phase: 05-visible-rolling-background-compaction
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - codex-rs/core/src/codex.rs
  - codex-rs/core/src/tasks/mod.rs
  - codex-rs/core/tests/suite/compact.rs
  - codex-rs/core/tests/suite/compact_remote.rs
autonomous: true
requirements:
  - RUN-03
user_setup: []
must_haves:
  truths:
    - Overlapping successful background compactions apply or drop in deterministic order without corrupting the live tail.
    - A failure from any still-relevant overlapping background compaction triggers the blocking fallback path exactly once and prevents later background jobs from mutating history afterward.
    - Turn teardown and cancellation still resolve remaining background jobs as aborted rather than producing double terminal outcomes.
  artifacts:
    - codex-rs/core/src/codex.rs
    - codex-rs/core/src/tasks/mod.rs
    - codex-rs/core/tests/suite/compact.rs
    - codex-rs/core/tests/suite/compact_remote.rs
  key_links:
    - `apply_completed_background_auto_compact_if_ready(...)` and `recover_failed_background_auto_compact_if_ready(...)` currently consume one completed job at a time.
    - `ContextManager::splice_compacted_prefix(...)` already defines the safe apply boundary and should remain the corruption guardrail.
    - `codex-rs/core/src/tasks/mod.rs` owns teardown and cancellation, so it must stay aligned with rolling terminal-outcome semantics.
---

<objective>
Make overlapping background compactions settle safely through ordered apply, stale-drop, and single-fallback recovery.

Purpose: after Plan 01 allows rolling overlap, the turn loop still needs deterministic rules for consuming several finished jobs. This plan wires overlap into the existing splice and fallback safety boundaries so rolling background compaction stays correct under both success and failure.
Output: successful overlapping background compactions settle safely, stale results are discarded deliberately, and any failure triggers the existing blocking fallback path exactly once.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-visible-rolling-background-compaction/05-RESEARCH.md
@.planning/phases/05-visible-rolling-background-compaction/05-VALIDATION.md
@codex-rs/core/src/codex.rs
@codex-rs/core/src/tasks/mod.rs
@codex-rs/core/tests/suite/compact.rs
@codex-rs/core/tests/suite/compact_remote.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drain completed rolling compactions in deterministic order</name>
  <files>codex-rs/core/src/codex.rs, codex-rs/core/tests/suite/compact.rs, codex-rs/core/tests/suite/compact_remote.rs</files>
  <action>Update the turn loop so it consumes completed rolling compactions in a deliberate order, applies successful results through the existing prefix-splice helper, and drops stale completions once they are no longer safe or useful to apply.</action>
  <verify>cargo test -p codex-core compact</verify>
  <done>Overlapping successful completions can settle without racing each other into the transcript.</done>
</task>

<task type="auto">
  <name>Task 2: Keep failure recovery single-shot under overlap</name>
  <files>codex-rs/core/src/codex.rs, codex-rs/core/src/tasks/mod.rs, codex-rs/core/tests/suite/compact.rs, codex-rs/core/tests/suite/compact_remote.rs</files>
  <action>Ensure that one failed rolling background compaction stops the turn, runs the existing blocking fallback exactly once, and cancels or neutralizes remaining tracked jobs so they cannot produce later history mutations.</action>
  <verify>cargo test -p codex-core compact && cargo test -p codex-app-server compaction</verify>
  <done>Overlap preserves Phase 4's failure contract instead of multiplying it.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p codex-core compact`
- [ ] `cargo test -p codex-app-server compaction`
- [ ] Overlapping successful completions either apply in the intended order or are dropped as stale
- [ ] One failed rolling background compaction triggers blocking fallback exactly once
- [ ] Remaining jobs cannot mutate history after fallback begins
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Rolling overlap now behaves deterministically for both success and failure paths
- This plan preserves Phase 2 splice safety and Phase 4 guardrails instead of replacing them
</success_criteria>

<output>
After completion, create `.planning/phases/05-visible-rolling-background-compaction/05-02-SUMMARY.md`
</output>
