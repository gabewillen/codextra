---
phase: 04-failure-recovery-and-blocking-guardrails
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - codex-rs/core/tests/suite/compact.rs
  - codex-rs/core/tests/suite/compact_remote.rs
  - codex-rs/app-server/tests/suite/v2/compaction.rs
  - codex-rs/app-server/README.md
autonomous: true
requirements:
  - COMP-01
  - COMP-02
user_setup: []
must_haves:
  truths:
    - Manual compaction remains a blocking operation in both core and app-server surfaces.
    - Pre-turn protective compaction remains blocking and preserves its current failure semantics.
    - Phase 4 does not accidentally introduce Phase 5 indicator or multi-compaction behavior.
    - Any observable API guidance that becomes explicit in Phase 4 is documented.
  artifacts:
    - codex-rs/core/tests/suite/compact.rs
    - codex-rs/core/tests/suite/compact_remote.rs
    - codex-rs/app-server/tests/suite/v2/compaction.rs
    - codex-rs/app-server/README.md
  key_links:
    - `codex-rs/core/tests/suite/compact.rs` already covers manual compaction behavior and local pre-turn failure semantics.
    - `codex-rs/core/tests/suite/compact_remote.rs` already covers remote pre-turn failure semantics.
    - `codex-rs/app-server/tests/suite/v2/compaction.rs` already exercises manual `thread/compact/start` and auto-compaction notifications.
---

<objective>
Preserve manual and pre-turn blocking guardrails while Phase 4 changes background failure recovery.

Purpose: Phase 4 is allowed to interrupt only failed background compactions. Everything else about manual compaction and pre-turn protection should remain stable. This plan locks those guardrails with direct regressions and docs if the app-server contract becomes more explicit.
Output: manual compaction and pre-turn protective compaction remain blocking, and app-server surfaces keep the same explicit contract.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-failure-recovery-and-blocking-guardrails/04-RESEARCH.md
@.planning/phases/04-failure-recovery-and-blocking-guardrails/04-VALIDATION.md
@.planning/phases/04-failure-recovery-and-blocking-guardrails/04-01-PLAN.md
@codex-rs/core/tests/suite/compact.rs
@codex-rs/core/tests/suite/compact_remote.rs
@codex-rs/app-server/tests/suite/v2/compaction.rs
@codex-rs/app-server/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock manual compaction as the unchanged blocking path</name>
  <files>codex-rs/core/tests/suite/compact.rs, codex-rs/app-server/tests/suite/v2/compaction.rs, codex-rs/app-server/README.md</files>
  <action>Add or tighten regressions proving manual `Op::Compact` and `thread/compact/start` still behave as blocking compaction entry points even after background failure recovery is introduced, and document any clarified app-server expectation if needed.</action>
  <verify>cargo test -p codex-app-server compaction</verify>
  <done>Manual compaction stays explicitly blocking across both core and app-server surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Lock pre-turn protective compaction as unchanged blocking behavior</name>
  <files>codex-rs/core/tests/suite/compact.rs, codex-rs/core/tests/suite/compact_remote.rs</files>
  <action>Extend no-regression coverage so local and remote pre-turn protective compaction still stop the turn on failure and do not inherit background-fallback semantics or new interruption markers.</action>
  <verify>cargo test -p codex-core compact</verify>
  <done>Pre-turn compaction remains blocking and failure-stopping exactly as before.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p codex-core compact`
- [ ] `cargo test -p codex-app-server compaction`
- [ ] Manual compaction remains blocking
- [ ] Pre-turn protective compaction remains blocking
- [ ] No Phase 5 indicator or overlap behavior leaks into Phase 4 changes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `COMP-01` and `COMP-02` remain explicitly protected by automated coverage
- App-server guidance matches the preserved manual-compaction contract if any surface wording changes
</success_criteria>

<output>
After completion, create `.planning/phases/04-failure-recovery-and-blocking-guardrails/04-03-SUMMARY.md`
</output>
