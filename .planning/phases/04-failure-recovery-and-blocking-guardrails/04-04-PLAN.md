---
phase: 04-failure-recovery-and-blocking-guardrails
plan: 04
type: execute
wave: 3
depends_on:
  - 04-02
  - 04-03
files_modified:
  - codex-rs/core/src/codex_tests.rs
  - codex-rs/core/tests/suite/compact.rs
  - codex-rs/core/tests/suite/compact_remote.rs
  - codex-rs/app-server/tests/suite/v2/compaction.rs
autonomous: true
requirements:
  - RECV-01
  - RECV-02
  - COMP-01
  - COMP-02
user_setup: []
must_haves:
  truths:
    - Phase 4 ends with automated proof that background auto-compaction now has exactly one terminal outcome per run.
    - Failure recovery remains scoped to failed background auto-compaction and does not regress successful background behavior.
    - Manual and pre-turn blocking semantics remain intact after the recovery changes land.
    - App-server compaction surfaces remain coherent with the new failure/fallback behavior.
  artifacts:
    - codex-rs/core/src/codex_tests.rs
    - codex-rs/core/tests/suite/compact.rs
    - codex-rs/core/tests/suite/compact_remote.rs
    - codex-rs/app-server/tests/suite/v2/compaction.rs
  key_links:
    - `codex-rs/core/src/codex_tests.rs` is the best place for focused terminal-outcome and interruption-order assertions.
    - `codex-rs/core/tests/suite/compact.rs` and `compact_remote.rs` are the end-to-end harnesses for local and remote compaction behavior.
    - `codex-rs/app-server/tests/suite/v2/compaction.rs` is the app-server compatibility harness for compaction-related notifications and manual RPC behavior.
---

<objective>
Lock Phase 4 behind final regression proof for recovery outcomes and guardrails.

Purpose: Once the core fallback path and guardrail-preservation slices land, the phase still needs one final wave that proves the whole contract end to end. This plan turns the Phase 4 requirement set into regression coverage that fails on double-terminal behavior, missed fallback, or guardrail drift.
Output: a final regression net for failure recovery and blocking guardrails across core and app-server surfaces.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-failure-recovery-and-blocking-guardrails/04-RESEARCH.md
@.planning/phases/04-failure-recovery-and-blocking-guardrails/04-VALIDATION.md
@.planning/phases/04-failure-recovery-and-blocking-guardrails/04-02-PLAN.md
@.planning/phases/04-failure-recovery-and-blocking-guardrails/04-03-PLAN.md
@codex-rs/core/src/codex_tests.rs
@codex-rs/core/tests/suite/compact.rs
@codex-rs/core/tests/suite/compact_remote.rs
@codex-rs/app-server/tests/suite/v2/compaction.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add focused terminal-outcome regression coverage</name>
  <files>codex-rs/core/src/codex_tests.rs</files>
  <action>Add focused core tests that prove a background auto-compaction run can resolve only as applied, failed-then-fallback, or aborted, and that failure recovery does not leave stale completed background state behind.</action>
  <verify>cargo test -p codex-core compact</verify>
  <done>Core tests fail if Phase 4 ever reintroduces double-terminal behavior or stale failed background state.</done>
</task>

<task type="auto">
  <name>Task 2: Add end-to-end recovery and guardrail regressions</name>
  <files>codex-rs/core/tests/suite/compact.rs, codex-rs/core/tests/suite/compact_remote.rs, codex-rs/app-server/tests/suite/v2/compaction.rs</files>
  <action>Add end-to-end local, remote, and app-server regressions proving failed background auto-compaction interrupts and falls back correctly while manual and pre-turn compaction remain blocking and successful background compaction remains non-interrupting.</action>
  <verify>cargo test -p codex-core compact && cargo test -p codex-app-server compaction</verify>
  <done>Phase 4 has broad regression proof across the supported compaction surfaces.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p codex-core compact`
- [ ] `cargo test -p codex-app-server compaction`
- [ ] Background auto-compaction resolves only as applied, failed-then-fallback, or aborted
- [ ] Successful background auto-compaction remains non-interrupting
- [ ] Manual and pre-turn compaction remain blocking
- [ ] App-server compaction surfaces remain coherent with the new failure recovery path
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 4 finishes with automated proof for `RECV-01`, `RECV-02`, `COMP-01`, and `COMP-02`
- The phase leaves visibility and multi-compaction overlap work entirely to Phase 5
</success_criteria>

<output>
After completion, create `.planning/phases/04-failure-recovery-and-blocking-guardrails/04-04-SUMMARY.md`
</output>
