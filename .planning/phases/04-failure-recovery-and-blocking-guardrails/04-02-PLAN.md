---
phase: 04-failure-recovery-and-blocking-guardrails
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - codex-rs/core/src/codex.rs
  - codex-rs/core/src/codex_tests.rs
  - codex-rs/core/tests/suite/compact.rs
  - codex-rs/core/tests/suite/compact_remote.rs
autonomous: true
requirements:
  - RECV-01
  - RECV-02
user_setup: []
must_haves:
  truths:
    - A failed background auto-compaction interrupts the active agent turn.
    - Recovery reuses the existing blocking compaction flow instead of duplicating local or remote summarization logic.
    - Successful background auto-compactions remain non-interrupting.
    - Failed background auto-compactions do not loop into repeated fallback or repeated background restarts in the same turn.
  artifacts:
    - codex-rs/core/src/codex.rs
    - codex-rs/core/src/codex_tests.rs
    - codex-rs/core/tests/suite/compact.rs
    - codex-rs/core/tests/suite/compact_remote.rs
  key_links:
    - `codex-rs/core/src/codex.rs` owns both `start_background_auto_compact(...)` and the existing blocking `run_auto_compact(...)` helper.
    - `codex-rs/core/tests/suite/compact.rs` and `compact_remote.rs` already cover successful non-interrupting background behavior and pre-turn failure behavior.
---

<objective>
Interrupt failed background auto-compactions and recover through the existing blocking compact path.

Purpose: The user requirement is that compaction never interrupts active agent work unless it fails. This plan adds that failure-only interruption path and routes recovery through the existing blocking compact implementation rather than inventing a second fallback compactor.
Output: failed background compactions stop the active turn, run blocking recovery once, and preserve the existing successful background behavior.
</objective>

<execution_context>
@/Users/gabrielwillen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/gabrielwillen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-failure-recovery-and-blocking-guardrails/04-RESEARCH.md
@.planning/phases/04-failure-recovery-and-blocking-guardrails/04-VALIDATION.md
@.planning/phases/04-failure-recovery-and-blocking-guardrails/04-01-PLAN.md
@codex-rs/core/src/codex.rs
@codex-rs/core/src/codex_tests.rs
@codex-rs/core/tests/suite/compact.rs
@codex-rs/core/tests/suite/compact_remote.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trigger failure-only interruption from the regular turn loop</name>
  <files>codex-rs/core/src/codex.rs, codex-rs/core/src/codex_tests.rs</files>
  <action>Teach the regular turn orchestration path to detect a failed completed background auto-compaction, interrupt the active turn in a turn-owned way, and avoid applying or reusing that failed result later.</action>
  <verify>cargo test -p codex-core compact</verify>
  <done>Background auto-compaction failure now changes control flow only on failure, not on success or cancellation.</done>
</task>

<task type="auto">
  <name>Task 2: Reuse the blocking compaction path for recovery</name>
  <files>codex-rs/core/src/codex.rs, codex-rs/core/tests/suite/compact.rs, codex-rs/core/tests/suite/compact_remote.rs</files>
  <action>Route failed background auto-compaction into the existing inline compaction helper with the correct mid-turn injection semantics, and add local/remote regressions proving fallback runs once and the active turn no longer continues past the failure point.</action>
  <verify>cargo test -p codex-core compact</verify>
  <done>Failed background auto-compaction recovers through the same blocking compaction implementation used elsewhere, with no bespoke fallback summarizer path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p codex-core compact`
- [ ] Failed background auto-compaction interrupts the active turn
- [ ] Recovery uses the existing blocking compaction path
- [ ] Successful background auto-compaction remains non-interrupting
- [ ] Fallback runs at most once per failed background compaction
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `RECV-01` is satisfied directly in core orchestration
- `RECV-02` now includes explicit no-loop / single-fallback behavior
</success_criteria>

<output>
After completion, create `.planning/phases/04-failure-recovery-and-blocking-guardrails/04-02-SUMMARY.md`
</output>
